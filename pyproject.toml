[project]
name = "multi-agent-spec-template"
version = "0.1.0"
description = "Multi-agent development template with Spec Kit, Claude, and Copilot"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
hello = "hello_world.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
